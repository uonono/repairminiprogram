var filter = {
  //截取字符串
  getString: function (string) {
    return string.slice(0, 1)
  },
  getTime: function (data) {

    return  data + 11
  }
}
// 导出对外暴露的属性
module.exports = {
  getString: filter.getString,
  getTime:filter.getTime
}